require('require')

module.exports.loop = function() {
	let Imperium = new IMPERIUM(Memory.Imperium || {})
	
	// Imperium.load()
	// Imperium.run()
	// Imperium.save()
	
	// Imperium.checkForSectors()
}